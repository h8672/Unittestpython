#Map

from mapobject import MapObject

class Map:
    def __init__(self):
        #Create list of map objects that you cant change after it's made
        # The so called tuple!
        this.important_list = ( MapObject("Earth", ["A", "B", "C", "D"])
